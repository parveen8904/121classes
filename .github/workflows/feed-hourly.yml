name: Hourly news feed
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL --retry 3 --retry-delay 20 "https://caparveensharma.com/api/cron/feed-hourly" || true
