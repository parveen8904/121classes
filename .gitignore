# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Env (never commit real keys)
.env
.env.local
.env*.local

# Misc
.DS_Store
*.log
.vercel
tsconfig.tsbuildinfo

# Playwright
/playwright-report/
/test-results/
.env*

# Firebase config for the Android app. It ships inside the APK and is not a
# secret, but it names the project and is regenerated from the console, so it
# is kept out of git rather than drifting in two places.
mobile/android/app/google-services.json
